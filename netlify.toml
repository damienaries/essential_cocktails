[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# React Router (client-side routes): serve index.html for unknown paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
