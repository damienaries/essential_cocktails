export function HomePage() {
  return (
    <div style={{ maxWidth: 820, margin: '0 auto', textAlign: 'left' }}>
      <h1 style={{ marginTop: 0 }}>Cocktails (React rewrite)</h1>
      <p>
        This is the new React app that will fully replace the Vue version. Next up: connect
        Firebase auth + data, then rebuild the cocktail list and admin CRUD as vertical slices.
      </p>
    </div>
  )
}

